import React, { FC } from "react";

import { Header } from "src/containers";

import { Wrapper } from "./styles";

const Main: FC = () => {
  return (
    <Wrapper>
      <Header />
    </Wrapper>
  );
};

export default Main;
